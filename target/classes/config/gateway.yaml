server:
  port: 8081

spring:
  cloud:
    gateway:
      routes:
        - id: evento
          uri: lb://evento-service/
          predicates:
            - Path=/evento/**
        - id: eventoCB
          uri: lb://eventoCB/
          predicates:
            - Path=/eventosCB/**
